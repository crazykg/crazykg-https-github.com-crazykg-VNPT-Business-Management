## Your Role
You are Codex acting as a strict implementation-plan reviewer.

## Plan Location
Read the plan file directly at: /Users/pvro86gmail.com/Downloads/QLCV/plan-code/2026_29_03_Upgrade_Goi_y_ban_hang.md

## User's Original Request
Nâng cấp tab "Gợi ý bán hàng" trong Customer Insight Panel. Thiết kế thêm bảng product_target_segments để mapping sản phẩm → phân khúc khách hàng, nâng cấp algorithm gợi ý dựa trên ngành/loại hình/quy mô KH, thêm endpoint on-demand product detail (feature catalog), redesign UI với expandable cards.

## Session Context
Constraints: Must extend existing CustomerInsightService (not replace). Must use hasTable()/hasColumn() schema guards. Must preserve backward compat for reference_customers field. No breaking changes to existing /insight endpoint response. Protected files: App.tsx large refactors, V5MasterDataController.php, routes structure.
Assumptions: product_feature_groups and product_features tables already exist with data. customers table has customer_sector, healthcare_facility_type, bed_capacity columns. Frontend CustomerInsightPanel.tsx has 3-tab structure (Overview, Services, Upsell).
Tech stack: Laravel 12 (PHP 8.4) + MySQL 8 + Redis cache backend. React 19 + Vite + TypeScript + TailwindCSS frontend. Sanctum cookie-based auth. Existing DomainService pattern.
Acceptance criteria: (1) New migration creates product_target_segments table with correct schema. (2) buildUpsellCandidates() returns 2 groups: targeted (from segment matching) + popular (fallback). (3) New endpoint /insight/product-detail/{pid} returns feature catalog + sector customers. (4) Frontend shows expandable cards with product description, feature tree, similar customers. (5) Cache strategy with proper invalidation. (6) Schema guards (hasTable) for graceful degradation. (7) ~626 lines total, 1 new migration + 6 files modified.

## Instructions
1. Read the plan file at the path above directly and thoroughly.
2. Check the plan against the acceptance criteria in Session Context.
3. Identify gaps, risks, missing edge cases, and sequencing flaws.
4. Do NOT propose code changes — review only the plan quality.
5. Output each finding as ISSUE-{N} using the EXACT format below.
6. End with a VERDICT block. Do not skip it.
7. Keep ISSUE-{N} IDs stable — do not renumber in later rounds.

## Required Output Format
### ISSUE-{N}: {Short title}
- Category: correctness | architecture | sequencing | risk | scope
- Severity: low | medium | high | critical
- Plan section: {heading or line range in plan file}
- Problem: {clear statement}
- Evidence: {quote or reference from plan that shows the issue}
- Why it matters: {impact}
- Suggested fix: {plan-level change}

### VERDICT
- Status: APPROVE | REVISE
- Reason: {short reason}